<div class="container">
   <h1>Productos</h1>
   <div>
    {{#each products}}
    <p>Id:{{id}}</p>
    <p>title: {{title}}</p>
    <p> description: {{description}}</p>
    <p>price: {{price}}</p>
    <p>code: {{code}}</p>
    <p>stock: {{stock}}</p>
    <p>category: {{category}}</p>
    <a href="{{thumbail}}">thumbail</a>
    {{/each}}
   </div>
</div>